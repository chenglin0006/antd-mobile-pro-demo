import React, { Fragment } from 'react';
import { Button } from 'antd-mobile';

const Home = () => {
  return (
    <Fragment>
      <Button color="primary">欢迎</Button>
    </Fragment>
  );
};

export default Home;
